stages:
  - name: test1
    steps:
      - echo "test"
      - sh "ls -la"
      - sh "env"
  - name: test2
    steps:
      - dir("."){sh "ls -la"}
      - script { echo "test" }
      - ezLog.info "info"
      - script { 
                  def sum=1+1;
                  echo "${sum}";
               }
