stages:
  - name: test1
    steps:
      - echo "test"
      - sh "ls -la"
      - sh "env"
  - name: test2
    steps:
      - dir("."){sh "ls -la"}
      - script{ echo "test" }
      - ezLog.info "info"
      - script{ echo "test1"
                echo "test2"}
